# FLY ROUND 25 — "GROUND & NIGHT" — PLAN OF RECORD (Fable, W0, 2026-09-11)

> This is the orchestration plan as approved before W0. The charters are the
> contract each agent works to; the recon ids point into `scripts/r25-recon.md`.
> Base: `main` = `f0cd81e`. Integration branch: `claude/happy-planck-dw8yyf`
> (the session's working branch; `main` advances only on the user's word).


## Context

The user flies the Satellite style with the always-on immersive treatment (Codex overhaul, `main` = `f0cd81e`). At ~3500 ft inside the volumetric clouds it feels immersive; **at 50–500 ft AGL the world reads flat, and at night the ground is a near-black photo with copper dot-trains for roads.** They also want the phone HUD's button column replaced by **one floating button that fans out every action** (joystick, throttle rail and boost pad stay).

What the code says (verified by three exploration passes, file:line in the reports):

- **Lighting**: exactly ONE directional light (`FlyScene.jsx:3292`) + ONE hemisphere (`:3289`), zero point/spot/ambient lights. `immersiveLighting()` (`lib/fly/immersive.js:19-38`) gives night key **0.09**, fill 0.10, env 0.14, bg 0.22. The moon only rotates the key direction (`ONE_SUN`, no phase, no intensity). Hemisphere ground colour `#5a6b53` (daytime olive) is never re-written. Night haze is driven to exactly 0 (`AERIAL_LAW.nightRamp`, `NIGHT_TRUTH_R23.hazeNight.retire 1.0`). Night bloom threshold is raised to 0.91 by `satellite-atmosphere.js:53`.
- **Copper striping**: `SATELLITE_VISUALS.lighting = { roadGlow: 0.035, streetPoolGain: 12, streamGain: 0, trafficGain: 0.32 }` (`lib/fly/satellite-visuals.js:12`) overrides the R16/R23-swept road intensity (2.4) by ~70× while raising the 42 m lamp-pool term to 12 (`night-city.js:57-64`, road fragment `world-bend.js:2607-2626`). Documented as a reaction to "broad white ribbons" (`GRAPHICS_OVERHAUL.md:110-115`), never re-swept. The road ADDS light to its own ribbon only; **nothing lights the terrain at night**. SatCityGlow unmounted, canopy/roofs have no night term, `NIGHT_CITY_R23.windows` dead.
- **Ground**: every AGL band is a cull band; nothing turns ON below ~600 m. Finest geometry = 8.5 m pole, 4.5 m car. Terrain material = three-tile `MeshStandardMaterial` with only `map` (no normal/detail); the only low-AGL term is a ±10% 5.5 m luma grain that `fwidth`-fades at grazing angles. One 1500 m shadow cascade (1.5–5.9 m/texel). N8AO radius 24 m, measured 0–6%. No DoF in satellite; ground rush forced off (`Effects.jsx:171`).
- **Mobile**: `TouchControls.jsx` has 8 cluster buttons + throttle + boost in `controls-right`; no overflow affordance; hangar reachable only via PauseMenu; harnesses freeze 9 `touch-*` testids, ≥44 px, 8 zone names, pairwise zone disjointness.

User rulings (asked, answered): ground = **low skimming 50–500 ft**, not touchdown; night target = **"real city from a small plane"** (lit street web, pavement pools, headlight streams, horizon halo, moonlit terrain, varied windows); mobile = **radial arc from one FAB, throttle+boost stay, minimap+contracts stay**; sixth agent = **F FEEL**; **every frozen ceiling stays, content-gated so Owens = 0 by construction**; user will **fly it AND run the harness list**.

## §0 Rulings

- **Frozen**: Owens ≤ 261 / satellite ≤ 375 / toy ≤ 480 draws; fixed-pose tris ≤ 2.0 M; soak p95 ≤ 2.2 M; textureBytes ≤ 300 MB; `gpuFrameMs` 12; boot reveal never lengthens; `WARP.flashMs` 250; `verify-flicker` bound 12 never moves; R21 quartet green; `verify-monuments-sat` frozen; 8 mobile zones, 44 px targets, pairwise disjointness (closed AND fan-open). Neon/toy byte-identical.
- **Venue truth**: `node_modules` ABSENT (npm install first); Esri/OFM/adsb 403-blocked → offline fixture only (`scripts/_fixture.js`, `FLY_TILE_FIXTURE=1`, :3199); Chrome absent → `node -r ./scripts/_pw-shim.js`; SwiftShader 1–3 fps. **Every fps/ms/tearing/bloom-feel number belongs to the user's RTX 5080 + phone.** A SwiftShader number never re-baselines a live number.
- **Content gate**: every +draw layer parks at `count 0 / visible=false` when its content set is empty (the `SatHouseLights.jsx:149-156` idiom); B's rig never mounts by day (`nightK === 0`), so every pinned-noon gate is untouched by arithmetic. The fixture desert scene has zero landcover/landuse ⇒ Owens = 0 by construction.
- **WORKER_PROTOCOL stays 20.** No agent needs a worker payload change (lamps from pole instance matrices, windows from `queryColumns`, hedges from `parcel-roads.js`, scrub from pooled SatTint geometry). Seven pin sites untouched; E fixes the stale "stays 18" label at `scripts/verify-night-city-identity.mjs:224`.
- **One-flag revert**: each feature behind its pre-seeded `enabled:false` block at the END of `lib/fly/fly-constants.js` (after :6890); flag-off byte-identical AND PROVEN (fixture pixel A/B / fingerprint / GLSL false-branch verbatim). Every new shader text ⇒ NEW FINAL cache key in the `world-bend.js` registry (the R24 `hillKey`/`r24VariantKey` token idiom at :1668-1675) AND a `prewarm.js` warm-set entry in the same change.
- **Runtime arming**: `lib/fly/r25-pins.js` `r25On(name, sub)` = `pinned(block, 'window.__fly<Name>Override')` (`lib/fly/fly-pins.js:22-26`) so harnesses and the user arm a block from the console, never by editing constants. `_boot.js` pins (both legs, :95-120 and :155-162) are added in the SAME commit that flips a block ON.

## §1 Protocol (reproduced from R24, `FLY_ROUND24_PLAN.md` §5 + `FLY_ROUND24_KICKOFF.md`)

- **Branches/worktrees**: `git worktree add ../skyloom-r25-<letter> -b r25/<letter> claude/happy-planck-dw8yyf` (the W0 tip); own `.next`; ports A 3130 / B 3131 / C 3132 / D 3133 / E 3134 / F 3135 (fixture 3199; never 3000/3002/3019/3022/3100/3105). Artifacts to `scripts/r25-out/` (gitignored). Agents never commit to `main` or to the integration branch; Fable merges into `claude/happy-planck-dw8yyf`.
- **Ledgers**: `scripts/r25-<letter>-<name>.md` — header with venue-truth blockquote; RED first, mechanism, fix, cost, frozen gates touched, decisions, open risks, "could not measure here".
- **Merge**: W2 order **E → A → B → C → D → F**, one reviewed merge each, E smoke (`scripts/r25-smoke.sh`, `SMOKE_NODE_ONLY=1` for the fast check) after each; subject `R25 W2 merge <n>/6 (Fable): <OWNER> — <what> (r25/<letter> <sha>)`. Arbitrated files (owner present): `FlyScene.jsx`, `Effects.jsx`, `world-bend.js` (`applyHillshade` GLSL order = A `'d'` then B `'n'`; tokens appended to `hillKey` in that order after R24's four), `FlyCanvas.jsx` rig mounts. Constants conflict ⇒ someone edited another owner's block ⇒ rejected. **Grep for the other owner's newest symbol before editing any shared harness file** (the R24 silent-revert lesson).
- **Every agent acts as Fable**: RED before GREEN, measured claims only, honest "not measurable here", no widening, one-flag revert, no API keys, per-source licensing, no r3f-perf, no `.next` sharing.

## §2 Shared substrate (W0, orchestrator) — the ground-bubble signal

A, C and F all key on "how low are we". One writer, three readers:

- `lib/fly/ground-bubble.js`: `bubbleK(aglVisM, cfg)` — smoothstep over `GROUND_BUBBLE.aglInM 500 → aglOutM 700` with `hysteresisM 60`, damped `smoothSec 0.6`; pure, node-testable.
- `components/fly/GroundBubbleRig.jsx` (the `HudSyncRig.jsx` shape), mounted from `FlyCanvas.jsx` beside `<HudSyncRig>` (:90) by ONE line; `useFrame` priority −49 (after FlyScene's −50 publishes `runtime.groundElevVis`); reads `eyeAglVis()` from `lib/fly/ground-vis.js` (never raw AGL — DEM refinement steps); publishes `runtime.groundBubble = { k, aglM, aglVisM }` in place + `window.__flyStats.groundBubble` (dev). Flag off ⇒ rig unmounted ⇒ consumers read `runtime.groundBubble?.k ?? 0`.

## §3 Charters

### A GROUND — `GROUND_DETAIL_R25` — "a bubble of detail under the aeroplane"
1. **Procedural detail overlay in the tile fragment** (`overlay`): inside `applyHillshade` (`world-bend.js:1412`, after the SAT_QUILT block :1580-1584), predicate-injected (`groundDetailOn()`, the `hillFragOn()` idiom :1658). Two octaves of the existing `hillVNoise` (:1524) at `scaleM [1.4, 0.45]` on `vWorldXZ` → a fine luma grain + an analytic normal perturbation (finite differences) feeding the fragment N·L already computed from `vHillNW` (:1538-1540), so relief moves with C's key light. Classed by the sampled imagery colour (grass hue/sat → fine high-amp; low-sat mid-luma asphalt → smooth sparse speckle; warm soil → medium). `uGroundDetail = k × strengthByTier` via `setGroundDetail(k)` (the `setMicroDetail` idiom :1692); `fwidth`-faded like R24's micro term. **0 draws**; new token `'d'` on `hillKey`; prewarm automatic.
2. **Scrub/grass cards** (`scrub`): ONE `InstancedMesh` in new `components/fly/SatGroundDetailLayer.jsx` (mounted in `worldRoot` by one line beside `SatClutterLayer` `FlyScene.jsx:3345-3347`); pool {high 2000, medium 800, low 0}; 2 crossed cards (4 tris) sharing the `immersive-foliage.js` leaf atlas; hash-stable anchors on the 2 s SatVeg cadence inside `radiusM 300`, sampled on the pooled SatTint landcover triangles (grass/farmland/wood via `SAT_TINT.palette`), rejected within 20 m of a `queryColumns` column, 6 m of a road quad, 4 m of sat-water; ground height from the clutter's bilinear grid (`CLUTTER.gridSegments 16`, `fly-constants.js:6104-6107`). Bend `applyBendAnchor` (no new key unless sway ⇒ `'world-bend-anchor-scrub-r25'` + PREWARM `SIZES` entry). **+1 draw where placed, 0 at Owens.**
3. **Hedgerows along parcel-road edges** (`hedges`): second instancer, pool {high 600}, 12-tri box segment 12 m × 1.6 m at 6 m outboard of cls 5/6 segments from `parcelRoadScan` (`lib/fly/parcel-roads.js`), skipping segments with a column within 10 m. **+1 draw, 0 at Owens** (one cls-1 highway). Fences NOT built (sub-pixel at 50 m).
4. **Low-AGL landcover drape lift** (`tint`): SatTint α `mix(0.1, 0.18, k)` on its existing cadence — CPU multiplier, no shader text.
5. **z19 below 150 m AGL** (`z19`, ships OFF): `tile-sources.js:78-82` `maxLevel` step keyed on k; measured at fixture Owens (R22 measured z18 = +12 draws at that pose) — flips only on a green ≤ 261 row + user z19 availability.
6. **`TERRAIN_LIGHT.workerNormals`** user A/B via `__flyTerrainLightOverride` (unreachable here — LERC path); a §6 knob, never a re-baseline (`verify-skirt-worker` identity leg goes RED by design with it on).

Files: `SatGroundDetailLayer.jsx` (new), the `'d'` block + `setGroundDetail` in `world-bend.js`, `SatTintLayer.jsx` α, `tile-sources.js`, ONE mount line in `FlyScene.jsx`. Gate `scripts/verify-ground-bubble.js` (E writes, A ships `window.__flyGroundDetail = { read, set }`): k ≥ 0.95 at 80 m / ≤ 0.02 at 900 m with hysteresis sweep; scrub count > 0 suburb, === 0 desert with draws == flag-off; hedges likewise; fixture pixel A/B at noon Powell 80 m |Δ| > 2/255 (RED = 0 on flag-off); no `'d'` token flag-off; `uGroundDetail === 0` at 3500 ft.

### B NIGHT — `NIGHT_GROUND_R25` — "a real city from a small plane"
**Chosen mechanism: a top-down NIGHT GROUND IRRADIANCE render target** (evaluated vs instanced pool sprites — reach only the ground, double-count the ribbon — and per-tile light lists — fill-rate cost where this venue is blind). **0 steady-state draws**, +≤20 only on an update frame (movement ≥ 150 m), 4 MB RGBA8 at high (1024²) / 512² medium / none low, one `texture2D` per receiver fragment inside `if (uNGGain > 0.0)` (0 by day ⇒ pinned-noon programs execute none of it).
1. **`components/fly/NightGroundRig.jsx`** (ONE mount line in `FlyCanvas.jsx`; renders nothing to the main scene): ortho camera straight down over `spanM 3000`, origin texel-snapped (the `snapToShadowTexel` lesson `FlyScene.jsx:236-246`), separable blur ping-pong (`blurPx 2`, key `'night-ground-blur-v1'`); updates only on ≥150 m movement or source-signature change, ≥1 s apart ⇒ a settled pose never updates. Publishes `runtime.nightGround` + exported `nightGroundUniforms = { uNGMap, uNGOrigin, uNGInvSpan, uNGGain }` from `world-bend.js` (the `atmoUniforms` holder idiom :1455).
   - Phase 1 roads: enable layer bit 7 on ready road meshes and render the MAIN scene through the ortho cam with `layers.set(7)` (never a bare Group — that drops `fog` from the program key and compiles a twin). **Risk to handle**: the bend vertex term must be zeroed (write the bend uniform 0, restore) for the RT pass. The existing night terms become 10–20 m of pavement spill after blur.
   - Phase 2 splats: one instanced additive quad mesh (`'night-ground-splat-v1'`, private Scene): lamps from the pole `InstancedMesh.instanceMatrix` (`SatClutterLayer.placeStatic`, B adds `runtime.satClutterPoles = mesh` one line at :503) radius 14 m `#ffb066`; windows from `queryColumns` (r + 8 m, ∝ nightK × gain × the architecture material's darkBuilding hash); porch pools from SatHouseLights anchors.
   - Phase 3 receivers: terrain (token `'n'`, `reflectedLight.indirectDiffuse += ng.rgb * diffuseColor.rgb * uNGGain` after `<lights_fragment_end>`); buildings (`satellite-architecture-material.js` key `|immersive-surface-v1 → -v2`, weight `exp(-facadeY/6)` so light climbs two floors); canopy (`cinematic-ground.js:102` `|night-ground-v1`); homes (:147); clutter (`SatClutterLayer.jsx:252-258` `onBeforeCompile`, key `'clutter-night-ground-v1'`) so cars sit in the pool light.
2. **Road re-sweep** (`roadResweep`): B's block SHADOWS `SATELLITE_VISUALS.lighting` via one branch in `satelliteRoadLighting()` (`night-city.js:57-64`): proposal `roadGlow 0.022, streetPoolGain 8, streamGain 0.6, trafficGain 0.32` — the pavement pool moves to the RT, the ribbon recedes, headlight streams exist again. All four are live uniforms ⇒ a console A/B on the user's machine, no reboot.
3. **Window variance on the CINEMATIC material** (`windows`): port `NIGHT_CITY_R23.windows`' three knobs (darkFrac 0.22, gain spread 0.45–1.55, tint jitter 0.18) into `satellite-architecture-material.js:121-130` as identity-default uniforms (the R23 atlas path is dead under the cinematic material — documented, not armed).
4. **Canopy/roof night** (`canopyNight`): the irradiance sample + C's moon key + a tiny `uCanopyNightLift 0.06`; roofs = irradiance × 0.3 + moon N·L.
5. **Horizon light pollution** (`halo`): a **SkyDome term** (not a remounted SatCityGlow — the overhaul's silhouette objection stands): `uCityGlow[4]` (az, strength, width) from the 4 strongest POI cities by pop/dist² within 90 km + an isotropic local band from splat energy within 3 km (CPU, 2 s cadence, 0 at Owens). 0 draws; "(no key) SkyDome" registry entry. B also ships `setSkyMoonPhase(illum)` in `SkyDome.jsx` — the seam C calls.
6. Grade seam: B never touches `immersiveLighting`, bloom or exposure (C's).

Files: `NightGroundRig.jsx`, `lib/fly/night-ground.js` (new, mirrors `night-city.js:41-51` arming), `'n'` block + holders in `world-bend.js`, `satellite-architecture-material.js`, `cinematic-ground.js`, `SkyDome.jsx`, `night-city.js:57-64` branch, two lines in `SatClutterLayer.jsx`, ONE mount line in `FlyCanvas.jsx`. Gate `scripts/verify-night-ground.js` (B ships `window.__flyNightGround = { arm, set, read, forceUpdate }`): flag-off RED (no rig, no `'n'`, no RT bytes); armed RT sized per tier, updates on a 200 m hop, none when settled 30 s; pixel A/B on a terrain crop 20–40 m beside an artery |Δ| > 2/255 and moving MORE than the ribbon crop (the spill is the claim); building-base crop moves; settled Manhattan night draws ≤ 375 == flag-off; textures delta ≤ 8 MB; NOON rig unmounted, programs flat; Owens night splats 0; windows knob moves a facade crop; halo > 0 Manhattan, 0 Owens.

### C LIGHT — `LIGHT_BUBBLE_R25` — "light with a source, a moon that is a light, shadows that fit the bubble" (ZERO shader text)
1. **AGL-keyed shadow ortho radius** (`shadow`): `mix(1500, 350, k)` quantised to 8 steps with `hysteresisM 40`, written imperatively on `sun.shadow.camera` in new `components/fly/LightBubbleRig.jsx` (ONE `FlyCanvas.jsx` mount); JSX boot props (`FlyScene.jsx:3303-3306`) untouched; `runtime.shadowRadiusM` read by the texel snap (:2966) and the near-receive reach (:769) — C's two one-line edits. 1.46 m → 0.34 m/texel at 2048. **A second shadow light REJECTED**: it changes the light count in every program key ⇒ recompiles every lit material.
2. **AGL-keyed N8AO** (`ao`): `aoRadius mix(24, 5, k)`, `intensity mix(5.0, 3.5, k)` on the retained pass (`Effects.jsx:701`, C adds `runtime.aoPass = p`, one line). High tier only. 0 draws.
3. **The moon as a LIGHT** (`moon`): `immersiveLighting()` gains `moon = { illum, up, k }` — synodic phase (`epochNewMoonMs Date.UTC(2000,0,6,18,14)`, `synodicDays 29.530588`), `up = smooth(-8, 0, antiSolarEl)` via `moonDirFromSun`; night key `0.09 → mix(0.025, 0.16, illum)·up + 0.02`, env night `mix(0.05, 0.10, illum·up) + 0.04`, fill `+0.04·illum·up`. **Written so `day === 1` yields the R24 expression bit-for-bit.** C calls B's `setSkyMoonPhase(illum)` (`?.`-guarded until B merges). Anti-solar + synodic phase, not an ephemeris (§7).
4. **Hemisphere ground/sky by time of day** (`hemi`): `hemi.groundColor` on the `keyMix` bucket cross-blend (day `#5a6b53`, dusk `#5a4a3e`, night `#1a2030`), night sky `#3d4c6e` — undersides stop being lit grass-green at midnight.
5. **Night grade** (`grade`, in `satellite-atmosphere.js:43-53` under C's flag): bloom threshold `mix(1.08, 0.78, night)` so B's pools/windows bloom; exposure `-0.10·night`; **haze floor** `max(0.12, atmoNightMul)` multiplied under C's flag at `FlyScene.jsx:2485-2489` (never editing `NIGHT_TRUTH_R23`/`AERIAL_LAW`) — a night city with zero aerial perspective is why it reads as a black photo.

Files: `LightBubbleRig.jsx` (new), `immersive.js`, `satellite-atmosphere.js`, `shadow-kernel.js`; 3 one-liners in `FlyScene.jsx`, 1 in `Effects.jsx`, ONE `FlyCanvas.jsx` mount. Gates: `scripts/verify-moon-light.mjs` (node: 200 daytime elevations bit-equal to R24; night varies with illum/up; new-moon floor) and `scripts/verify-shadow-bubble.js` (fixture: radius 1500 at 3500 ft, 350 ± 1 step at 80 m, at most one step across a 480→560→480 m sweep, programs flat, `verify-one-sun` and `verify-shadow-calm` still green, hemi ground per bucket, AO radius follows k).

### D MOBILE — `MOBILE_FAN_R25` — one FAB, a quarter-arc fan
New `components/fly/hud/TouchFan.jsx`, rendered by `TouchControls.jsx` inside `controls-right` in place of the persistent row (:524-535) and both contextual rows (:490-523). **No new zone** (8-zone contract holds; petals are pointer-events-auto descendants of `controls-right`).
- **FAB** `data-testid="touch-fab"`, `aria-expanded`, 56 px portrait / 48 px landscape, lucide Menu/X rotating 45°, `.hud-glass` (phone override keeps it solid).
- **Petals** = `ActionButton` (:310-338) verbatim at 44 px; every existing testid preserved + **`touch-hangar`** (`setHangarOpen(true)`, the `PauseMenu.jsx:203-206` path — hangar had no key and no touch button). Persistent order `look, atlas, logbook, photo, hangar, pause`; contextual `inspect, intercept, cinema` per the existing rules (:449-457) on the inner slots so the persistent set never shifts.
- **Geometry**: arc 180° → 90°, radius 168 px portrait / 150 landscape, second ring +52 px for petals 7–9; framer-motion spring (stiffness 520, damping 34, mass 0.8, stagger 22 ms) with `AnimatePresence`; `readReducedMotion()` ⇒ duration 0.
- **Close** on FAB tap, tap outside (window `pointerdown` capture that only READS the event — no fight with the R17 tap-leak rule), any petal tap, any `covered` transition (:377-383; the `if (covered) return null` at :413 already unmounts; an effect resets `open`).
- **Layout**: portrait column = anchor 3.25 + rail 8.25 + 0.75 + boost 4.25 + 0.75 + FAB 3.5 = 20.75 rem ⇒ **`infoChip.dockBottomRem` 24.5 → 21.75** (`fly-constants.js:2815`, derivation comment rewritten; flag-off string character-identical). Landscape: FAB beside the throttle+boost row (`flex-row-reverse` :538), column stays 11.5 rem, minimap untouched, fan opens up-left over the free centre band.
- **`hideWhileOpen: ['info-dock','toasts']`**: `data-fan-open="1"` on the fly root + `globals.css` visibility rules — the 180° petal lands on the chip's row in both orientations. Minimap, contracts chip, stick, throttle, boost stay visible and MUST stay disjoint from every petal (gate).
- **BoostBar ring** unaffected (pad stays mounted; ring measures `[data-testid="touch-boost"]` every 100 ms).
- **Constants**: `MOBILE_UI.cluster` gains `hangar`; `clusterSize` gains `fabPx`, `petalPx`. Flag-off: `TouchFan.jsx` not imported; today's rows byte-for-byte.
- **Harness edits (E makes, D reviews)** via new `openFan(page)/closeFan(page)` in `scripts/_mobile-boot.js` (no-op when no FAB ⇒ every gate runs on both trees): `verify-mobile.js` :161-169, :214, :225, :231, :333, :345-359 (+hangar), :365-393, :425-429 (+`touch-fab` tap-leak row), :481, :508, :526, :547, :585-603 (+hangar), :607-636 (+fab, closed and open), :655-661; `verify-mobile-layout.js` :482 (`openFan` first) and :326-351 (+ a second disjointness census with the fan OPEN); `verify-hangar.js:431` (+ gate 14b: `touch-hangar` opens the hangar and hides the stick); `verify-logbook.js:484`. New `scripts/verify-mobile-fan.js`: closed = only FAB/stick/throttle/boost mounted; open = 6 petals ≥44 px on the arc inside the viewport, both orientations; tap-outside closes; `covered` closes+unmounts; contextual petals follow an injected lock; reduced-motion ⇒ final positions frame 1; BoostBar ring tracks the pad while open; zero pageerrors.

### E CERT — "a green means something here AND there"
1. Step 0: `npm install`; fixture health `FLY_TILE_FIXTURE=1 node -r ./scripts/_pw-shim.js scripts/verify-fixture.js` at `FLY_BOOT_SCALE=6`; `SMOKE_NODE_ONLY=1 scripts/r24-smoke.sh` on the R25 base = the baseline row of `scripts/r25-e-cert.md`.
2. `scripts/r25-smoke.sh` (r24's shape :62-82 + NOT-COVERED note): node rows + `verify-r25-flagoff.mjs`, `verify-moon-light.mjs`, re-based `verify-artifact-hygiene.mjs`; browser rows + the six new gates, each `content_gate`.
3. New gates, RED-calibrated on the flag-off tree first: `verify-ground-bubble.js`, `verify-night-ground.js`, `verify-moon-light.mjs`, `verify-shadow-bubble.js`, `verify-mobile-fan.js`, `verify-feel-ground.js`, `verify-r25-flagoff.mjs` (every R25 block off ⇒ no R25 token in any `customProgramCacheKey`, every R25 GLSL false-branch verbatim — the `verify-c-flagoff.mjs` idiom), `verify-registry-inventory.mjs` (every cache-key literal appears in the world-bend header or is "(no key)"). Each states which pin it releases and proves the released term reachable in that tier.
4. D's harness edits (table above) + `openFan/closeFan`.
5. Re-baselines: `verify-artifact-hygiene.mjs:40-42` → `R25_BASE` = the W0 sha, `PATTERNS` + `'scripts/r24-*'`; `verify-night-city-identity.mjs:224` "stays 18" → "stays 20". The 18 harnesses sending a dead `?graphics=` param and the dead `__flyVisualsArm` handle: **document in `scripts/r25-recon.md`, do not edit** (no owner; `IMMERSIVE_VALIDATION.md:1-12` retired the switch).
6. Fixed-pose pixel gates un-recertified since the overhaul (`verify-sat-night` 27/1/1, `verify-dusk`, seam hashes, `verify-monuments-sat`, NEON_COVER hashes): FIXTURE column re-baselined on the flag-off R25 base with RED-first PNG pairs under `scripts/r25-out/`; live columns ONLY from user-pasted numbers; unmeasurable ⇒ NOT CALIBRATED, never PASS.
7. `scripts/r25-user-diag.md` on W1 day 1 (the round's real RED): Powell 40.1578/−83.0752 at 80–150 m AGL 2 min day (`copy(__flyStats.frame)`); the same at night via `window.__flySunOverride` (`copy(__flyStats.night)` + three screenshots Powell 80 m / Columbus 250 m / Manhattan 792 m); phone portrait+landscape screenshots. Then `scripts/r25-close-sweep.md` §2 run list (`| Gate | Runs here? | What the user's machine adds | Command |`).

### F FEEL — `FEEL_R25` — "the ground rushes, the lens finds it, the air changes" (0 draws except DoF)
1. **Ground rush in satellite** (`groundRush`): `Effects.jsx:171` rule becomes `… && !(sat && immersiveOn('camera') && !feelOn('groundRush'))`; the feed (:590-621) multiplies `strength` by `runtime.groundBubble.k` so cruise stays exactly 0. +0 draws (merged EffectPass); prewarm automatic (buildPassList is the single source).
2. **Near-field DoF at satellite high** (`dof`): a `'sat-dof'` descriptor beside `'toy-dof'` (:293-330) using `patchDofDepth` (DEPTH_FIX :118-132), added to `PRE_CURVE` (:434); focus = camera→aircraft distance, `focusRange mix(220, 60, k)`, `bokehScale mix(0, 0.9, k)` (kept SUBTLE — the ground detail is the point, a §6 taste knob); ≈ +4 fullscreen draws at sat-high whenever mounted — **Owens post-overhaul reads 249–252, so 256 ≤ 261 is tight: measured on the fixture before any flip; medium/low never mount (phones capped at medium).**
3. **Low-AGL chase settle** (`chase`, `chase-camera.js`): `posLambda × mix(0.85, 1.06, k)` (:159), `bankShare × mix(0.7, 0.42, k)` (:190), FOV `62 + (−2)·k` (:267), `+3 m·k` height bias; via `setChaseBubble(k)` called by the rig — no FlyScene touch; the R18 IEEE-identity rule (:248-259) holds at k = 0.
4. **AGL-keyed audio** (`audio`, `immersive-audio.js:31-44`): wind `× (1 + 0.35·k)`, engine low-pass opens with k, ground-bounce send `× (1 + 0.12·smooth(60 m → 0))`.
5. **Terrain-proximity cue** (`proximity`): below 30 m AGL a filtered noise hiss at gain 0.08 rising to the deck (lazy third node). No HUD element.

Files: `chase-camera.js`, `cinema-camera.js`, `immersive-audio.js`, `audio-engine.js`, `juice.js`, `SpeedLines.jsx`; three hunks in `Effects.jsx` (arbitrated with C's one line). Gate `scripts/verify-feel-ground.js`: rush > 0 at 60 m + boost, exactly 0 at 3500 ft and cruise; `'sat-dof'` in the pass list at sat-high with bokeh 0 above the band; fixture Owens draws ≤ 261 with DoF mounted; chase FOV 60 at 80 m / 62 at 3500 ft; flag-off pass-list id sequence identical; `verify-chase-cam` / `verify-photo` unchanged.

## §4 Ownership matrix

| Agent | Owns (exclusive) | May touch (one-line mounts only) | Forbidden |
|---|---|---|---|
| A GROUND | `SatGroundDetailLayer.jsx` (new); `GROUND_DETAIL_R25`; the `'d'` block + `setGroundDetail` + `groundDetailOn()` in `applyHillshade` (contiguous, BEFORE B's); `SatTintLayer.jsx` α; `tile-sources.js` z19 read | `FlyScene.jsx:3347` one mount line; `prewarm.js` SIZES entry if a new attribute | `immersive.js`, `night-city.js`, `Effects.jsx`, shadow/AO, other blocks |
| B NIGHT | `NightGroundRig.jsx`, `lib/fly/night-ground.js` (new); `NIGHT_GROUND_R25`; `nightGroundUniforms` + NG GLSL + `'n'` block (AFTER A's); `satellite-architecture-material.js`; `cinematic-ground.js` keys; `SkyDome.jsx` + `setSkyMoonPhase`; `night-city.js:57-64` branch | `FlyCanvas.jsx` one mount; `SatClutterLayer.jsx` two lines | `immersive.js`, `satellite-atmosphere.js`, any light object, `Effects.jsx`, `FlyScene.jsx`, `satellite-visuals.js` (shadowed, never edited) |
| C LIGHT | `LightBubbleRig.jsx`; `LIGHT_BUBBLE_R25`; `immersive.js` moon terms; `satellite-atmosphere.js:43-53` under its flag; `shadow-kernel.js` | `FlyCanvas.jsx` one mount; `FlyScene.jsx` :2966, :769, `runtime.sunLight` (3 lines); `Effects.jsx:702` (1 line) | `world-bend.js` GLSL, `SkyDome.jsx` text, `NIGHT_TRUTH_R23`, `AERIAL_LAW` |
| D MOBILE | `TouchFan.jsx` (new); `TouchControls.jsx`; `MOBILE_FAN_R25`; `MOBILE_UI.cluster/clusterSize/infoChip.dockBottomRem/zones['info-dock'].phonePort`; `globals.css` `[data-fan-open]`; `FlyMode.jsx` attribute | `InfoCard.jsx` if the dock string moves | any `verify-*`, `_mobile-boot.js`, any 3D/shader file |
| E CERT | every `scripts/verify-*`, `_boot.js`, `_mobile-boot.js`, `_fixture.js`, `r24-fixture/*`, `r25-smoke.sh`, `r25-user-diag.md`, `r25-close-sweep.md` | dev-handle CONTRACTS (asks owners; never app code) | app source outside `scripts/` (except the :224 label), constants |
| F FEEL | `FEEL_R25`; `chase-camera.js`, `cinema-camera.js`, `immersive-audio.js`, `audio-engine.js`, `juice.js`, `SpeedLines.jsx`; the `'sat-dof'` descriptor + `PRE_CURVE` + the :171/:575 rules | `Effects.jsx` hunks (arbitrated) | `FlyScene.jsx`, `world-bend.js`, light/shadow, `immersive.js` |
| Fable (W0) | `GROUND_BUBBLE` block, `ground-bubble.js`, `GroundBubbleRig.jsx` + its mount, `r25-pins.js`, world-bend registry header entries, `scripts/r25-recon.md`, `FLY_ROUND25_PLAN.md`, `FLY_ROUND25_KICKOFF.md` | arbitrates `FlyScene.jsx` / `Effects.jsx` / `world-bend.js` / `FlyCanvas.jsx` at merge | — |

Seam rules: `applyHillshade` order A `'d'` → B `'n'`; `immersiveLighting` is C's only (B reads `runtime.immersiveLighting.night` for nightK); moon: C computes, B's setter writes; `runtime.groundBubble` one writer, read by A/C/F.

## §5 W0 scaffolding (ONE orchestrator commit on `claude/happy-planck-dw8yyf`; agents branch from it)

1. `npm install` (verify the lockfile installs; no new deps — framer-motion and n8ao present).
2. Append after `lib/fly/fly-constants.js:6890`, banner in the R24 wording (:4651-4658), owner-tagged, each with the byte-noop sentence:
   - `GROUND_BUBBLE = { enabled:false, aglInM:500, aglOutM:700, hysteresisM:60, smoothSec:0.6 }` (Fable, shared)
   - `GROUND_DETAIL_R25 = { enabled:false, overlay:{enabled:true, strengthByTier:{high:1,medium:0.7,low:0}, scaleM:[1.4,0.45], normalAmp:0.35, lumaAmp:0.06, classify:{grassHue:[70,160], grassSatMin:0.12, asphaltSatMax:0.08, asphaltLuma:[0.18,0.55]}}, scrub:{enabled:true, poolByTier:{high:2000,medium:800,low:0}, radiusM:300, cardM:[0.6,1.4], urbanAvoidM:20, roadAvoidM:6, waterAvoidM:4, landcover:['grass','farmland','wood'], cadenceSec:2}, hedges:{enabled:true, poolByTier:{high:600,medium:0,low:0}, offsetM:6, segM:12, heightM:1.6, columnAvoidM:10}, tint:{enabled:true, lowAglAlpha:0.18}, z19:{enabled:false, aglM:150}, workerNormals:{enabled:false} }` (A)
   - `NIGHT_GROUND_R25 = { enabled:false, rt:{sizeByTier:{high:1024,medium:512,low:0}, spanM:3000, updateMoveM:150, minUpdateSec:1, blurPx:2}, sources:{roads:{enabled:true, gain:1, spillM:16}, lamps:{enabled:true, gain:1, radiusM:14, color:'#ffb066', pool:900}, windows:{enabled:true, gain:0.6, radiusPadM:8, pool:2048}, houses:{enabled:true, gain:0.5}}, receivers:{terrain:true, buildings:{enabled:true, heightFalloffM:6}, canopy:true, homes:true, clutter:true}, roadResweep:{enabled:true, roadGlow:0.022, streetPoolGain:8, streamGain:0.6, trafficGain:0.32}, windows:{enabled:true, darkFrac:0.22, gain:[0.45,1.55], tintJitter:0.18}, canopyNight:{enabled:true, lift:0.06}, halo:{enabled:true, cities:4, rangeM:90000, gain:0.35, widthRad:0.35, heightK:0.12, localGain:0.25, color:'#ffb066'} }` (B)
   - `LIGHT_BUBBLE_R25 = { enabled:false, shadow:{enabled:true, radiusLowM:350, radiusHighM:1500, steps:8, hysteresisM:40}, ao:{enabled:true, radiusLowM:5, intensityLow:3.5}, moon:{enabled:true, keyNew:0.025, keyFull:0.16, envNew:0.05, envFull:0.10, fillGain:0.04, epochNewMoonMs:Date.UTC(2000,0,6,18,14), synodicDays:29.530588, discBrightness:[0.12,0.5]}, hemi:{enabled:true, ground:{day:'#5a6b53', dusk:'#5a4a3e', night:'#1a2030'}, skyNight:'#3d4c6e'}, grade:{enabled:true, nightBloomThreshold:0.78, nightExposureStops:-0.10, hazeNightFloor:0.12} }` (C)
   - `MOBILE_FAN_R25 = { enabled:false, fabPx:{portrait:56, landscape:48}, petalPx:44, radiusPx:{portrait:168, landscape:150}, ringGapPx:8, arc:{fromDeg:180, toDeg:90}, spring:{stiffness:520, damping:34, mass:0.8, staggerMs:22}, hideWhileOpen:['info-dock','toasts'], closeOnCover:true, order:['look','atlas','logbook','photo','hangar','pause'], contextual:['inspect','intercept','cinema'], dockBottomRem:21.75 }` (D)
   - `FEEL_R25 = { enabled:false, groundRush:{enabled:true, aglBandM:120, boost:1.2}, dof:{enabled:true, tier:'high', focusRangeM:[60,220], bokeh:0.9}, chase:{enabled:true, posLambdaMul:1.25, bankShareMul:0.6, fovDeltaDeg:-2, heightBiasM:3}, audio:{enabled:true, windGain:0.35, groundBounceGain:0.12, bounceAglM:60}, proximity:{enabled:true, aglM:30, gain:0.08} }` (F)
3. `lib/fly/r25-pins.js` (`r25On`), `lib/fly/ground-bubble.js`, `components/fly/GroundBubbleRig.jsx` + ONE `FlyCanvas.jsx` mount line. No `_boot.js` pins yet (nothing is ON).
4. `world-bend.js` registry header: R25 inventory stubs (`'d'`/`'n'` hill tokens, `'world-bend-anchor-scrub-r25'` conditional, `'night-ground-splat-v1'`, `'night-ground-blur-v1'`, `'clutter-night-ground-v1'`, `|immersive-surface-v2`, `|night-ground-v1` ×2, "(no key) SkyDome").
5. `scripts/verify-artifact-hygiene.mjs` re-based to the W0 sha; `.gitignore` + `scripts/r25-out/`.
6. Docs: `scripts/r25-recon.md` (the three exploration reports + this design, file:line), `FLY_ROUND25_PLAN.md` (this plan in the R24 §0–§7 shape), `FLY_ROUND25_KICKOFF.md`, a one-line `CLAUDE.md` notice ("R25 in flight").
7. Node smoke on the scaffold: `SMOKE_NODE_ONLY=1 scripts/r24-smoke.sh` green; `git diff --stat` shows constants append + new files only.

## §6 Waves and the agent launch

- **W0**: §5, by me, on `claude/happy-planck-dw8yyf` (the integration branch this round); agents branch `r25/<letter>` from it.
- **Day 1**: spawn **E first** (npm install, fixture health, `openFan/closeFan` no-op helper, `r25-user-diag.md` → handed to the user immediately). Then A, B, C, D, F in ONE message (parallel), each an Opus agent (`model: "opus"`, `run_in_background: true`) whose prompt opens **"You are Fable, master game developer, acting as owner <LETTER> <NAME> of Round 25"** and carries verbatim: the charter above, the ownership row, the rules block (worktree/branch/port, never `main`, never another agent's server, artifacts to `scripts/r25-out/`, one-flag revert PROVEN, new shader text ⇒ new FINAL key + PREWARM entry, RED before GREEN, ledger sections, no API keys / licensing / no r3f-perf / no `.next` sharing, grep for the other owner's newest symbol before touching a shared harness file), the venue truth, the frozen numbers, and the dev-handle contracts it must ship for E (`__flyGroundDetail`, `__flyNightGround`, `__flyStats.shadow`, `__flyDof`, `touch-fab`/`touch-fan[data-open]`). B ships `setSkyMoonPhase` + empty `nightGroundUniforms` holders by day 2 so C/A compile against real names.
- **W1**: agents build behind flags; E calibrates every new gate RED on the flag-off tree; ledgers current. I answer seam questions via SendMessage, never edit their worktrees.
- **W2**: merges into `claude/happy-planck-dw8yyf` E → A → B → C → D → F, E smoke after each; conflicts arbitrated with the owner present.
- **W3**: fixture fleet on the integrated tree; user runs §7's list and the checkpoints; **flag flips ON only on green** (each flip commit adds the `_boot.js` pin on both legs); `FLY_ROUND25.md` (§0 why · §1 headline symptom→closed defect with the measurement · §2 waves · §3 per-agent shipped · §4 certification with an "unmeasurable here" column · §5 postmortem · §6 user checkpoints · §7 lessons · §8 flag ship state); `scripts/r25-close-sweep.md`; the `CLAUDE.md` top notice; push `main` only from the integrated smoke-green tree; push `claude/happy-planck-dw8yyf` throughout as the session's working branch.

## §7 Verification

**Here (fixture, SwiftShader)**: `SMOKE_NODE_ONLY=1 scripts/r25-smoke.sh` after every merge; full `scripts/r25-smoke.sh 3134` on the integrated tree; each new gate RED on flag-off then GREEN armed; `verify-r25-flagoff.mjs` proves byte identity; frozen ceilings at the fixture desert/city poses with EVERY R25 switch armed (Owens ≤ 261 incl. F's DoF, satellite ≤ 375, programs flat across AGL sweeps and fan opens); R21 quartet; mobile trio in both orientations, closed and open.

**User's machine (RTX 5080 Chrome + phone)**, checkpoints in this order (performance feel FIRST — the R20 §6.15 lesson):
1. R25 BASE diag pack: `copy(__flyStats.frame)` at Powell 80–150 m AGL day and night — the round's RED.
2. Night "before" screenshots at Powell 80 m / Columbus 250 m / Manhattan 792 m.
3. Phone screenshots of today's HUD; then D's fan demo from `r25/d` as soon as W1 closes (petal size, arc, spring, chip hiding).
4. Ground bubble by day at 80 m over Powell: overlay reads as ground texture or as noise; scrub density; hedge scale.
5. Night irradiance A/B via `__flyNightGround.set()` sweeps (roadGlow / lamp / window / halo) — the "real city from a small plane" verdict; then the road re-sweep.
6. Moon + grade: full vs new moon via `__flySunOverride`, hemi ground, bloom threshold, haze floor.
7. Shadows/AO in the bubble: the 350 m frustum edge visible or not; AO radius at the deck.
8. Feel: ground rush, DoF, chase settle, audio — reduced-motion on and off.
9. 15-minute satellite soak with everything on; frame stats pasted.

Run list rows (`scripts/r25-close-sweep.md` §2.7): `verify-ground-bubble` · `verify-night-ground` · `verify-moon-light.mjs` · `verify-shadow-bubble` · `verify-mobile-fan` / `verify-mobile` / `verify-mobile-layout` · `verify-feel-ground` · `verify-r25-flagoff.mjs` · `verify-artifact-hygiene.mjs` · R21 quartet + `soak-fly` 15 min (user only) · `verify-sat-night` / `verify-dusk` / seam hashes (fixture column here, live column from the user). Prelude: `npm install && npm run dev -- -p 3019`, then `FLY_URL=http://localhost:3019 node scripts/<gate>`.

## §8 Not this round

TAA/TAAU; PBR terrain or buildings; three CSM or a second shadow-casting light; a lunar ephemeris; touch-down/landing/ground handling; z19 as a shipped default (built OFF, measured); the R23 `NIGHT_CITY_R23.windows` atlas path (dead under the cinematic material — documented); editing `NIGHT_TRUTH_R23` / `AERIAL_LAW` / `SATELLITE_VISUALS.lighting` (shadowed under R25 flags, never edited); deleting the dead `?graphics=` param or `__flyVisualsArm` (documented); the R24 refuted theories; any fps/ms number from SwiftShader; a WORKER_PROTOCOL bump.

## Critical files

- `lib/fly/toy-world/world-bend.js` — `applyHillshade` :1412-1646, `hillKey`/`r24VariantKey` :1668-1675, road fragment :2585-2670, registry header :12-401
- `components/fly/FlyScene.jsx` — lights :3289-3312, shadow rig :1005-1042 / :2843-2979 / snap :2966 / receive :769, immersive apply :2736-2741, night ramp :2485-2489, worldRoot mounts :3314-3351
- `components/fly/FlyCanvas.jsx` — rig mounts :73-94 (GroundBubbleRig, NightGroundRig, LightBubbleRig)
- `components/fly/Effects.jsx` — speed rule :171/:575, `'toy-dof'` :293-330, `PRE_CURVE` :434, N8AO :701, feed :590-621
- `lib/fly/immersive.js` :19-38 · `lib/fly/satellite-atmosphere.js` :43-53 · `lib/fly/satellite-visuals.js` :12 · `lib/fly/night-city.js` :41-64
- `components/fly/hud/TouchControls.jsx` :377-383, :430-457, :490-541 · `lib/fly/fly-constants.js` `MOBILE_UI` :2588-2871 (dock :2815), append point :6890
- `scripts/verify-mobile.js`, `scripts/verify-mobile-layout.js`, `scripts/verify-hangar.js:431`, `scripts/verify-logbook.js:484`, `scripts/_mobile-boot.js`, `scripts/_boot.js`, `scripts/r24-smoke.sh`, `scripts/verify-artifact-hygiene.mjs`
- Protocol sources: `FLY_ROUND24_PLAN.md` §3–§7, `FLY_ROUND24_KICKOFF.md`, `scripts/r24-close-sweep.md` §2.7, `lib/fly/fly-pins.js`
